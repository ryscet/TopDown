OpenData loads raw logs (ony converted from eeglab to csv) and saves them as hdf

SaveFormats opends HDF and saves numpy and mne

Independent analysis computes welch on all trial types and looks for differences between independednt samples

Other scripts are old versions currently unused I believe
